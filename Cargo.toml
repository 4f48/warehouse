[package]
name = "object-storage"
version = "0.1.0"
edition = "2021"

[dependencies]
axum = { version = "0.7.9", features = ["multipart", "http2"] }
tokio = {  version = "1.42.0", features = ["macros", "rt-multi-thread"] }
